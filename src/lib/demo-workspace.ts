/**
 * A fixed, well-known workspace id. `prisma/seed.ts` loads its demo data into
 * this workspace, and the app defaults to it on a fresh browser — so running
 * `npm run db:seed` and opening the app is enough to see a populated CRM,
 * with no id to copy from a terminal into the workspace switcher first.
 */
export const DEMO_WORKSPACE_ID = 'a0000000-0000-4000-8000-000000000001';
